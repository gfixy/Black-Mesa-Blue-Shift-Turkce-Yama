import QtQuick 2.9
import CrowbarCollective 1.0

LetterboxButton {
    focusedColor: Theme.colors.buttonText
    selectedColor: Theme.colors.highlight
    focusShapeOpacity: 1
    height: Math.ceil(64 * Theme.heightScale)
}
